CREATE TABLE BUDDYINFO (ID NUMBER(19) NOT NULL, NAME VARCHAR, PHONENUMBER NUMBER(19), ADDRESSBOOK_ID NUMBER(19), PRIMARY KEY (ID))
CREATE TABLE ADDRESSBOOK (ID NUMBER(19) NOT NULL, PRIMARY KEY (ID))
ALTER TABLE BUDDYINFO ADD CONSTRAINT FK_BUDDYINFO_ADDRESSBOOK_ID FOREIGN KEY (ADDRESSBOOK_ID) REFERENCES ADDRESSBOOK (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMBER(19), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
